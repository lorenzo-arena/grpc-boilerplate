syntax = "proto3";

package boiler.plate;

/**
 * The gRPC service
 */
service Boilerplate {
    rpc UnaryRequest (Request) returns (Response) {}
}

message Request {
    string text = 1;
}

message Response {
    string text = 1;
}
